def fatorial(int n) : int {
// vari ́avel s ́o pela divers~ao
var boolean lessThanZero;
// n <= 0, s ́o pela divers~ao
lessThanZero = n < 0 || n == 0;
if lessThanZero
then
return 1;
else
return n*fatorial(n-1);
endif
}