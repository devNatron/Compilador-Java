def fatorial(int n) : int {
// variavel só pela diversão
var boolean lessThanZero;
// n <= 0, só pela diversão
lessThanZero = n < 0 || n == 0;
if lessThanZero
then
return 1;
else
return nfatorial(n-1);
endif
}

def imprima(int before, int valor, String after) {
var int i;
i = 0;
while i < before do
print("");
i = i + 1;
endw
println("");
println(valor);
println(after);
}

def main {
imprima( 50, fatorial(5)2fatorial(3), "       fim");
}