def secondAdd (int c, int d): int{
  return c + d;
}

def firstAdd (int a, int b): int{
  var int x;
  x = 1;

  while x != 4 do
    a = secondAdd (a,b);
    x = x + 1;
  endw

  return x;
}

def main{
  println (firstAdd (10,20));
}
